
<%= form_with(model: [ @article, @article.comments.build ], local: true) do |form| %>
  <p>
    <%= form.label :body, "Nota" %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit 'Enviar' %>
  </p>
<% end %>